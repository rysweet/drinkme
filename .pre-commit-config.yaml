repos:
  - repo: local
    hooks:
      - id: docs-contract-tests
        name: Run documentation contract tests
        entry: python3 -m unittest discover -s tests -v
        language: system
        pass_filenames: false
